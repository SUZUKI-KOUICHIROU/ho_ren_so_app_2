<% provide(:title, @project.project_name) %>

<div class="container" style="margin-top: 20px;">
  <h1><%= "■#{@project.project_name}■"%></h1>
  <h2><%= "<ここにプロジェクトの概要なんか載せたらどうだろう。>"%></h2>
</div>

<hr color="#eeeeee">
</pre>

<div style="display:flex">
  <div style="padding: 20px; border-right: solid; width: 20%;">
    <%= link_to "報告する", new_project_report_path(@project), class: "btn btn-primary button-p10", style: "margin: 10px;" %>
    <br>
    <%= link_to "連絡する", new_project_message_path(@project), class: "btn btn-secondary", style: "margin: 10px;" %>
    <br>
    <%= link_to "相談する", new_project_counseling_path(@project), class: "btn btn-success", style: "margin: 10px;" %>
  </div>

  <div style="margin: 10px;">
    <div>


 <%
=begin%>
 <h2 style="text-align: left;">▼新着情報▼</h2>
      <p><%= "<ここにプロジェクトに関する通知を入れてみるとか。>"%></p>
      <p><%= "<例）○月○日分が未報告です。>"%></p>
      <p><%= "<例）あなたの報告へリアクションがありました。>"%></p>  
<%
=end%>

    </div>

    <span><%= "▼あなた宛の連絡(最新の#{@messages.count}件)▼" %></span>
    <table>
      <thead>
        <th>概要</th>
        <th>連絡者</th>
      </thead>

      <tbody>
        <% @messages.each do |message| %>
          <tr>
            <td>
              <%= link_to message.title, project_message_path(@project,message) %>
            </td>
            <td>
              <%= @member.find(message.sender_id).user_name %>
            </td>
          </tr>
        <% end %>
      </tbody>
    </table>
    <%= link_to "連絡一覧", project_messages_path(@project,current_user) %>

    <br>
    <span><%= "▼あなた宛の連絡(最新の#{@counselings.count}件)▼" %></span>
    <table>
      <thead>
        <th>概要</th>
        <th>相談者</th>
      </thead>

      <tbody>
        <% @counselings.each do |counseling| %>
          <tr>
            <td>
              <%= link_to counseling.title, project_counseling_path(@project,counseling) %>
            </td>
            <td>
              <%= @member.find(counseling.sender_id).user_name %>
            </td>
          </tr>
        <% end %>
      </tbody>
    </table>
    <%= link_to "相談一覧", project_counselings_path(@project,current_user) %>
  </div>
</div>
<%= link_to "報告フォーマットを編集する", edit_project_report_format_path(@project), class: "btn btn-primary button-p10", style: "margin: 10px;" %>
