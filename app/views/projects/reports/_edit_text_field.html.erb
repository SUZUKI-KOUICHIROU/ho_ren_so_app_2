<div class="container">
<%= TextField.find_by(question_id: answer.question_id).label_name %>
  <%= f.fields_for "answer[#{c}]", answer do |m| %>
  <p style="padding-top: 5px;"><%= m.text_field :value %></p>
  <% end %>
</div>