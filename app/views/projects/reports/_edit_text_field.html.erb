<div class="container">
<%= TextField.find_by(question_id: pattern.question_id).label_name %>
  <%= f.fields_for "answer[#{c}]", mo do |m| %>
  <p style="padding-top: 5px;"><%= m.text_field :value %></p>
  <% end %>
</div>