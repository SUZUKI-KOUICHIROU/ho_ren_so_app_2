<div class="container">
  <%= CheckBox.find(pattern.question_id).label_name %>
  <br>
  <% CheckBox.find(pattern.question_id).check_box_option_strings.each do |box| %>
   <%= f.check_box "answer[#{c}]", {multiple: true, include_hidden: false}, box[:option_string] %><%= box.option_string %>
  <% end %> 
</div>