<% if @counseling_c.present? %>
	<% buf = @counseling_c.counseling_confirmation_flag == true ? 'hand-thumbs-up-fill.svg' : 'hand-thumbs-up.svg' %>
	<%= link_to embedded_svg(buf), read_user_project_counseling_path(current_user,@project,@counseling), method: :patch,  remote: true %>
<% end %>
