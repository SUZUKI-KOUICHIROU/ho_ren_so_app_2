<% if @message_c.present? %>
	<% buf = @message_c.message_confirmation_flag.present? ? 'hand-thumbs-up-fill.svg' : 'hand-thumbs-up.svg' %>
	<%= link_to embedded_svg(buf), read_user_project_message_path(current_user,@project,@message), method: :patch,  remote: true %>
<% end %>
