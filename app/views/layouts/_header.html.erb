<nav class="navbar navbar-expand-lg navbar-light header shadow p-3 bg-white rounded justify-content-center">
  <% if user_signed_in? %>
    <%= link_to "Horenso_App", root_path, id: "logo", class: "navbar-brand" %>
    <% flash.each do |key, value| %>
      <%= content_tag(:a, value, class: "flash flash_#{key}") %>
    <% end %>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
  <% else %>
    <%= link_to "Horenso_App", root_path, id: "logo", class: "navbar-brand" %>
    <% flash.each do |key, value| %>
      <%= content_tag(:a, value, class: "flash flash_#{key}") %>
    <% end %>
  <% end %>
  <div class="collapse navbar-collapse justify-content-end" id="navbarSupportedContent">
    <ul class="navbar-nav">
      <% if user_signed_in? %>
        <li class="nav-item dropdown">
          <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
            <%= current_user.user_name %>
          </a>
          <div class="dropdown-menu dropdown-menu-right" aria-labelledby="navbarDropdown">
            <%= link_to "ユーザー設定", user_path(@user), class: "dropdown-item" %>
            <%= link_to "ログアウト", destroy_user_session_path, method: :delete, class: "dropdown-item" %>
          </div>
        </li>
      <% end %>
    </ul>
  </div>
</nav>
